syntax = "proto3";

message DimensionData {
    message Key {
        string PK = 1;
        int64 SK = 2;
    }

    message Value {
        message ChainData {
            map<string, double> protocolVersion = 1;
        }

        int64 eventTimestamp = 1;
        map<string, ChainData> chainData = 2;
    }

    Key key = 1;
    Value value = 2;
}